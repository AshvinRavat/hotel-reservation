// changes

// searching, filtering, allow to insert total guests & childe adults as well,
// add more details about rooms
// on rooms give quantity so customer can identify how many rooms available
// adding link in the rooms


// get check-in, check-out dates, & total_guest,
    // if total_guest are pending then default = 2

// get count for the booked dates, & Room ID(s)
    select count (id), room_id
    check start_date <= user_start_date,
    and end_date >= user_end_date

// compare with  : 

